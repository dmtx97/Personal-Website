version: "3.8"
services:
  node-webserver:
    container_name: "web-server"
    build: "./"
    ports:
      - "3000:3000"

  # pg:
  #   image: "postgres:12"
  #   container_name: "postgres"
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - ./pg/db_data:/var/lib/postgresql/data

